console.log('in js');;

$(document).ready(handleReady);
let increment = 0;
function handleReady(){
    $(document).on('click', '#generate', handleClicks)
}
//ready funcntion created
function handleClicks (

)